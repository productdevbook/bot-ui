overwrite: true
hooks:
  afterAllFileWrite:
    - prettier --write

schema:
  - http://localhost:8080/v1/graphql:
      headers:
        x-hasura-admin-secret: "braksadminsecretkey"
generates:
  # types/graphql.d.ts:
  #  plugins:
  #    - typescript-graphql-files-modules
  types/types.ts:
    plugins:
      - typescript
      - typescript-operations
